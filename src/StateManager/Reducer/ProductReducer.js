import * as ActionType from '../Action/actionList'
const ProductReducer=(state,action)=>{
    switch (action.type) {
    
        default:
            return null

    }
}

export default ProductReducer;